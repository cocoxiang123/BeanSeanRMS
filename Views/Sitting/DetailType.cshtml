@model ReservationSystem.Models.SittingType

@{
    ViewData["Title"] = "DetailType";
}

<h1>DetailType</h1>

<div>
    <h4>SittingType</h4>
    <hr />
    <dl class="row custom-list">
        <dt class = "col-sm-2">
            @Html.DisplayNameFor(model => model.Description)
        </dt>
        <dd class = "col-sm-10">
            @Html.DisplayFor(model => model.Description)
        </dd>
    </dl>
</div>
<div> 
    <a asp-action="EditType" asp-route-id="@Model.Id" class="btn btn-danger">Edit</a> |
    <a asp-action="SittingTypeList" class="btn btn-danger">Back to List</a>
</div>
